# Handoff — M0.9 + M0.10 complete; **M0 is closed** (2026-07-13)

- **M0 §6 is fully ticked.** Next box is **M1.1** (PyBOP gradient regression, `simulateS1` L-BFGS-B pinned). Commits: `d0e82e6`, `d79d194`, `4259caa` (M0.9), `45aff36` (M0.10). CTest is now **58** (a new self-containment gate), Debug/Release/CUDA all 58/58, and all 53 Catch2 binaries stayed digit-identical throughout both milestones.
- **M0.9 (9C-5):** `SpmFactory.hpp` no longer includes `SpmPipeline.hpp` — the params/layout value types split into `{Sei,SurfaceCrack,Lam,LithiumPlating}Params.hpp`, `SpmBatchLayout.hpp`, `AgeingModelMask.hpp`. A public TU fell 26 → 21 core headers with all eight ageing/pipeline kernels gone. Every core header has an `@surface api|support|internal` tag; `tests/structural/p9c5_public_surface.cmake` forbids api/support → internal includes, restricts bindings to api, and pins both declaration levels of every api AND support header.
- **The overclaim to NOT repeat:** the public TU is **not kernel-free**. `SpmScalarKernels.hpp` still reaches it via `CompiledCurve`'s inline `linearInterpolate`, because removing it would duplicate physics and break PC-10. Corrected in CHANGELOG/PLAN/report after review caught it.
- **Adversarial review earned its keep:** it broke the first gate seven ways (public method / enumerator / `*Params` field addable unseen; kernel reachable via `../` path or `<>` include; binding could include `core/detail/`) and found a real defect — `SeiParams.hpp`/`SurfaceCrackParams.hpp` used `slide::Status` without including it and compiled only by include-order luck. All fixed; **20/20 mutations red**; new compiler-verified gate `structural_test_core_9C5HeaderSelfContained` compiles all 28 api/support headers standalone.
- **M0.10 (9C-6):** no dead code exists in `src/core` (that is the finding). Split `SpmFactory.cpp`→`SpmBatch.cpp`, `PackSolver.cpp`→`PackSolverIterative.cpp`, `AsyncRecorder.cpp`→`AsyncRecordingCodec.cpp`+`detail/AsyncRecordingFormat.hpp`, `Recorder.cpp`→`RecordingFormat.cpp`, extracted `SpmDiffusionRhs.hpp`, deduplicated `checkedAdd`/`checkedMultiply` into `detail/CheckedArithmetic.hpp`, and turned the 1,281-line coverage reporter into `scripts/slide_coverage/`. 5/5 mutations red.
- **Core lines GREW 17,363 → 17,595 (+1.3%)** — registered as a prediction *before* the work, against PLAN's expectation of a reduction. Only `SpmPipeline.hpp` (745) and `CyclerV2.cpp` (771) exceed 700, both justified in writing.
- **UNMET, do not inherit it a third time:** the oversized TEST files (`core_ParserAllocation` 1,229, `core_PackSolver` 1,198, `core_Experiment` 1,168, `core_AsyncRecorder` 1,118, `core_ParameterSet` 1,007) are untouched. M0.8 grew them, M0.10 swept only production. **Give this its own box.**
- **Environment gotchas:** the CUDA lane needs MSVC on PATH (`/c/Program Files/Microsoft Visual Studio/18/Community/VC/Tools/MSVC/14.50.35717/bin/Hostx64/x64`), or nvcc fails with `Cannot find compiler 'cl.exe'`. `build-release/bin` still holds **stale 2026-07-12 `*_fast`/`*_ipo` binaries** outside the Ninja graph — one FAILS an assertion and is not evidence; delete or reconfigure that directory before trusting any glob-based measurement. Never `git checkout` to undo a mutation while work is uncommitted (it silently reverted real edits once).
- **Not claimed:** TSan not rerun; the Linux llvm-cov coverage lane not rerun after the reporter split (its self-test, census, and a ruff undefined-name check are the evidence; the CI path filter now includes `scripts/slide_coverage/**`). `ruff` was added as a dev dependency via `uv add --dev`.
